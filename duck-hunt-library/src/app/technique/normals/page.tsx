import ContentPage from '@/app/components/content-page';

const pageInfo = {
  contentId: 'technique/normals',
  title: 'Normals'
};

export default async function Page() {
  return <ContentPage pageInfo={pageInfo} />;
}
